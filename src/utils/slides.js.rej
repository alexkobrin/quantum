diff a/src/utils/slides.js b/src/utils/slides.js	(rejected hunks)
@@ -21,9 +17,10 @@ export function createSlides( length = 10, sig = 0 ) {
-	return Array.apply( null, Array( length ) ).map( ( value, index ) => {
-		index = sig || index;
-		 console.log($store.getters);
-		return {
-			src: `${ IMAGE_URL }?sig=${ index }`,
-			alt: `Image ${ index }`,
-		};
-	} );
-}
\ No newline at end of file
+export function createSlides(length = 10, sig = 0) {
+  return Array.apply(null, Array(length)).map((value, index) => {
+    index = sig || index;
+    console.log($store.getters);
+    return {
+      src: `${IMAGE_URL}?sig=${index}`,
+      alt: `Image ${index}`
+    };
+  });
+}
